/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["fakestoreapi.com","api.lorem.space","picsum.photos","firebasestorage.googleapis.com"],
        }
}

module.exports = nextConfig
